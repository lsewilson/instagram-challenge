<% if @posts.any? %>

  <% @posts.each.with_index do |post, index| %>
    <%= link_to image_tag(post.image.url(:medium)), post_path(post)%>
    <%= post.caption %>
  <% end %>

<% else %>
No posts added yet
<% end %>
